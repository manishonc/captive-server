services:
  portal:
    build:
      context: ./portal
    container_name: portal
    environment:
      - NODE_ENV=production
      - PORTAL_DOMAIN=${PORTAL_DOMAIN}
      - SERVER_HOST=${SERVER_HOST}
    networks:
      - captive_net
    restart: unless-stopped

networks:
  captive_net:
    external: true
