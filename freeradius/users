# AP session timeouts are now managed dynamically via Firebase.
# FreeRADIUS calls the server REST endpoint (/radius/authorize) on each auth.
# This file is kept as a safety fallback only.

# Fallback: accept with default timeout if REST module is unavailable
DEFAULT Auth-Type := Accept
	Session-Timeout = 36000
